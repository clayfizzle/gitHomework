class doJob {
    public static void main(Sting args[]){
        float time;
        float n;    
        float split = 0.5;
        float numJobs; 
        float through = sumJobs();
        float turnAround = .5*n*time ;
        float answer;
        float io          = t/2;
        float process     = t/2;
        
        answer = doIt(numJobs, t);

        System.out.println(answer);        
    }

    public float doIt(float numJobs, float t){
        (numJobs/2 * n*time) + .5*n*time;
    }
}
